interface:
  display_name: "OCI Agent Push"
  short_description: "Create OCIR repos and push verified images"
  default_prompt: "Use $oci-agent-push to create an authorized OCIR repository and push a verified local image."
policy:
  allow_implicit_invocation: true
